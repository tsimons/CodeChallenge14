describe('', function () {
  it('', function () {
      expect(countDown()).toBe('');
      expect(countDown()).toBe('');
      expect(countDown()).toBe('');
  });

  it('', function () {
      expect(countDown()).toBe('');
  });
});